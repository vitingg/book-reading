proxíma etapa: Organizar as rotas, quando logar você vai de fato pra rota né, então se for adm vai de "auth/login"
pra "/dashboard/adm"

proxíma etapa: autenticar e verificar, instalar o zod pra fazer todas as verificações

proxíma etapa (opcional): arrumar as rotas da API